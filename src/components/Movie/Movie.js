import React from 'react';

const Movie = ({movie}) => {
    console.log(movie); 
    return (
        <div className='text-center'>
            <h1>Movie</h1>
        </div>
    );
};

export default Movie;